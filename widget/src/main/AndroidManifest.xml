<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="android.mobile.widget">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name"
                 android:supportsRtl="true" android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 自定义下拉刷新上拉加载更多demo展示 -->
        <activity android:name=".ui.pullrefresh.PullRefreshActivity" />
        <activity android:name=".ui.pullrefresh.ShowActivity" android:label="@string/title_activity_show" android:theme="@style/AppTheme.NoActionBar"></activity>
        <!-- end -->


    </application>

</manifest>